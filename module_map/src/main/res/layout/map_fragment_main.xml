<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.activity.fragment.MainFragment">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/map_course_back"
            android:layout_width="10dp"
            android:layout_height="0dp"
            android:layout_marginStart="8dp"
            android:adjustViewBounds="false"
            android:fitsSystemWindows="false"
            app:layout_constraintBottom_toBottomOf="@+id/map_et_search"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/map_et_search"
            app:srcCompat="@drawable/map_ic_back"
            tools:ignore="ContentDescription" />

        <EditText
            android:id="@+id/map_et_search"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="24dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/map_shape_search_background"
            android:gravity="center_vertical"
            android:hint="@string/map_search_edit_hint"
            android:imeOptions="actionNext"
            android:inputType="text"
            android:maxEms="250"
            android:maxLength="180"
            android:maxLines="1"
            android:paddingStart="18dp"
            android:paddingTop="12dp"
            android:paddingEnd="18dp"
            android:paddingBottom="12dp"
            android:textColor="@color/transactionCompletionEditViewColor"
            android:textColorHint="@color/transactionCompletionEditViewColor"
            android:textSize="@dimen/map_search_edit_text_size"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/map_course_back"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="搜索一个地点" />

        <fragment
            android:id="@+id/map_nav_inner"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginTop="8dp"
            app:defaultNavHost="true"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/map_et_search"
            app:navGraph="@navigation/map_nav_graph_inner" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</FrameLayout>